# Backend API URL
# Local dev:   http://localhost:8080
# Production:  https://vrmm.eng.uwo.ca/api   (includes /api — matches Spring Boot context-path in prod)
VITE_API_URL=http://localhost:8080

# Western CAS logout endpoint (same URL in every environment)
VITE_CAS_LOGOUT_URL=https://ssocas.uwo.ca/cas/logout
