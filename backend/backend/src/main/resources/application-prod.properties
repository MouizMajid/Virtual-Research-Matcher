
# Production profile — activate with: --spring.profiles.active=prod

# All routes will be prefixed /api/* — Nginx proxies /api/ → 127.0.0.1:8080
server.servlet.context-path=/api

# Never auto-migrate in production
spring.jpa.hibernate.ddl-auto=validate

# OIDC / SSO (Western University via WTS IT)
# Uncomment and fill in after receiving client credentials post-domain-go-live
# spring.security.oauth2.client.registration.western.client-id=${OIDC_CLIENT_ID}
# spring.security.oauth2.client.registration.western.client-secret=${OIDC_CLIENT_SECRET}
# spring.security.oauth2.client.registration.western.scope=openid,profile,email,affiliation
# spring.security.oauth2.client.registration.western.redirect-uri={baseUrl}/login/oauth2/code/western
# spring.security.oauth2.client.registration.western.authorization-grant-type=authorization_code
# spring.security.oauth2.client.provider.western.issuer-uri=https://login.microsoftonline.com/${OIDC_TENANT_ID}/v2.0
